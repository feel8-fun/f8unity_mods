[project]
name = "f8unitymods-tools"
version = "0.1.0"
description = "Tooling metadata for F8UnityMods scripts and UI packaging"
requires-python = ">=3.13"
dependencies = [
  "PySide6>=6.8.0",
  "pyinstaller>=6.11.0",
]
